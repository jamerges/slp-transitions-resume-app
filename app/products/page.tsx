import ProductsPage from "@/components/ProductsPage";

export const metadata = {
  title: "Every tool is $9, for a limited time | SLP Transitions",
  description: "Find clarity on your next steps with real career paths. The free Module 0, the Pivot Report and the Career Pivot Suite, one payment each.",
  robots: { index: false, follow: true },
};

export default function Page() { return <ProductsPage />; }
