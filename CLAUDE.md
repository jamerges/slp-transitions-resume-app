# SLP Career Pivot Suite — project guide for Claude

Paid web tool + funnel for SLPs (speech-language pathologists) leaving clinical work.
Live at **https://app.slptransitions.com** (also slp-transitions-resume-app.vercel.app).
Marketing/WordPress site: **slptransitions.com** (SiteGround; Kadence theme; separate from this repo).
Owner: James Berges (jamoberges@gmail.com) — SLP → growth marketer; hosts the Xceptional Leaders podcast.

## The product ladder (strategy: content/product-strategy.md)
1. Free career quiz → `/quiz` (email-gated result, tags MailerLite with `quiz_result`)
2. **$9 Pivot Report** — quiz result → **Stripe** → `/report` collects the resume *after* payment. Pay-first is deliberate: the pre-payment resume upload was the funnel's biggest drop (people take the quiz on a phone, nowhere near their resume). `report-finalize` returns `needsIntake` for a paid session with no resume, and emails a link back so a phone buyer can finish from a laptop.
3. **$24 Career Pivot Suite** — resume + job posting → free preview → Stripe → `/success` (full package)
4. Later: Transition OS ($79-99), cohort, employer layer via podcast. One ICP, one funnel — park anything else.

## Stack & architecture
- Next.js 16 App Router, TypeScript, React 18, inline-style design system (tokens in `app/globals.css` `:root` — **never rely on inline `--var` style objects; React's server renderer drops them**).
- Anthropic SDK server-side only (`lib/anthropic.ts`, model `claude-sonnet-4-6`). System prompt = research-backed rules: banned AI-tell words, never fabricate numbers (bracketed placeholders), push→pull burnout reframing, factual guardrails (Epic cert needs employer sponsorship; MSL is doctorate-gated; UXR saturated).
- Stripe Checkout, **LIVE MODE since 2026-07-31** — production charges real cards. Webhook endpoint exists + verifies signature. `lib/stripe-guard.ts` (`assertKeyPriceMatch`) fails the request if key mode and price mode ever diverge, so a half-switched env surfaces loudly instead of silently. Live $24 price: `price_1TzOQ0KyPrmclvwmStUeaCoj` (also the in-code fallback in `app/api/checkout/route.ts`). The $9 live price comes from `STRIPE_REPORT_PRICE_ID`. Old test IDs (`price_1TRlBQ…` $24, `price_1Tz6Tj…` $9) are historical and are what `KNOWN_TEST_PRICES` still guards against. **Env var names differ between the two products** — `STRIPE_PRICE_ID` ($24) vs `STRIPE_REPORT_PRICE_ID` ($9); updating one and assuming the other followed is how the $24 flow broke on launch day.
  - **⚠️ Stripe's live webhook endpoint is `https://app.slptransitions.com/api/webhooks/stripe`** (destination `we_1TzOkiKyPrmclvwmjIANZSII`, API version 2020-08-27). The fulfilment handler was written at `/api/webhooks` on 2026-08-15 and Stripe was never repointed, so for three weeks every sale hit the old log-only route, returned 200, and delivery stayed dependent on the buyer's browser tab. Seven real sales, zero ops alerts, four $9 buyers never reached the customer group. Fixed 2026-09-03: `/api/webhooks/stripe/route.ts` re-exports the fulfilment handler, so both paths serve the same code. **A webhook is only verified when a real Stripe event produces an ops alert in James's inbox** — a synthetic POST to the route proves nothing about whether Stripe calls it. Unsigned requests are now rejected (400); the old "accept unverified in dev" branch is gone.
- **There is no test-mode webhook endpoint, on purpose (2026-09-15).** `we_1Twt9ZKyPrmclvwmmGCCUclp` at the old `vercel.app` hostname (created 2026-07-24) was deleted after Stripe's "failing webhook" email: every test-mode event (all of them from the local $19 end-to-end runs on 2026-09-11/12) hit the production deployment, which verifies with the LIVE signing secret, and was correctly refused with 400. Local testing fulfils through the browser path. If the webhook path ever needs exercising in test mode, use `stripe listen --forward-to localhost:3000/api/webhooks/stripe`; do not create a hosted test endpoint again.
- **2026-09-03 replay outcome:** all four unaccounted $9 buyers (Aug 19, 20, 23, 25) came back `needsIntake` — none had ever uploaded a résumé, so nothing was ever generated. `report-finalize` (re)sent each the add-your-resume link, gated by `claimOnce`. That is 4 of 6 $9 buyers stalling after payment: the phone-buyer drop the pay-first flow anticipated, with no reminder behind it. Recover URLs are `/report?session_id=<cs_live…>`; session ids are in the Stripe event JSON (`data.object.id`). Stripe's own "resend event" is unavailable past 15 days — POSTing `{sessionId}` to `/api/report-finalize` is the durable recovery path.
- **Stalled-report reminders** (2026-09-03, cadence changed 2026-09-15): `app/api/cron/stalled-reports` runs daily via `vercel.json` (15:00 UTC). It lists paid `pivot_report` sessions 1–14 days old, skips delivered/has-resume, sends the first `sendReportReminderEmail` once per session (`claimOnce("reminder48:…")`, key kept from the 48h era so nobody is reminded twice) and a shorter final nudge at day 5 (`nudge: 2`, `claimOnce("reminder5d:…")`), then an ops alert listing both. Six of the first eight $9 buyers never uploaded a résumé, which is why James moved it from 48h to 24h and added the second nudge. **It refuses to run without `CRON_SECRET`** — live since 2026-09-03 (route returns 401 without the header, 500 if the var is missing). Registered in Vercel → Settings → Cron Jobs (Hobby: 1-hour flexible window; a **Run** button there triggers it on demand). ⚠️ Vercel env-var gotchas that cost an hour: (1) the value must be header-safe — a pasted trailing newline fails the *build* in ~3s with `control character (0x0a) at position N`; type it or `openssl rand -hex 32 | tr -d '\n' | pbcopy`; (2) Vercel only validates and header-attaches the exact name `CRON_SECRET` — a misspelled key (`CHRON_SECRET` happened) passes the build silently and the route sees nothing; (3) Edit cannot rename a key — delete and re-add; (4) env changes need a fresh deployment (`git commit --allow-empty` works). Also seen on the env page: `STRIPE_suite_PRICE_ID` is a dead name (code reads `STRIPE_PRICE_ID`; the live price is hard-coded as fallback), and the four real API keys are stored as Config not Secret.
- **Post-purchase cross-sells exist on both paid products** and should stay priced and clear: the $9 report ends with "Ready to start applying? · Try it free · $24 →" carrying the résumé and top path into the Suite (`?continue=<sid>&path=`), and the Suite's success page ends with "Applying somewhere else too?" (same carry-over, next posting) plus a link to `/jobs`. A 2026-09-15 review wrongly assumed neither existed; check `ReportResults.tsx` and `FullResults.tsx` before adding another.
- **Backing out of checkout keeps the work (2026-09-24, from a critique pass with the design lenses).** Before this, browser Back from Stripe reloaded `/quiz` at question 1 and Stripe's back link went to the Suite's welcome step, so a hesitant buyer lost the result (quiz) or the résumé, posting and free preview (Suite). Now: the quiz saves its result to `sessionStorage` (`slp:quiz:result:v1`, this tab only) and restores it on any load; `report-checkout` takes `returnTo: "quiz"` and cancels to `/quiz?canceled=1` (wizard report buyers still cancel to `/`); the Suite saves a draft (`slp:suite:draft:v1`) just before redirecting and restores on `?canceled=1` or a back_forward load; both listen for `pageshow` with `persisted` so a bfcache restore doesn't come back stuck on "Opening checkout…"/"Redirecting". Production sends `no-store`, so Back is a fresh load there; **the dev server serves a cached, unhydrated page on Back, so test Back on prod, not locally.** Same pass: `report-checkout` prefills `customer_email` from the quiz gate (buyers typed it twice before); both checkouts carry `custom_text.submit` with the refund line; the gate says `COMPANY_COUNT` (passed from the server page as `companyCount`, so the client skips the DB) "health and ed-tech companies that value clinical skills", has linked labels and an inline error instead of a silently disabled button; the result shows the range and timeline as real text under the card (the card's pay line is ~8px on a phone); `ProductMenu` has a `quiet` prop (outline buttons) used under every quiz offer so each offer keeps one filled button; the duplicate "staring at a posting?" Suite pitch in the report card was cut (the menu lists the Suite once); CTA labels use `·` not em dashes and read prices from `priceOf`. Verified on prod the same day: browser Back from the live Stripe page restores the result, and the live checkout shows the prefilled email and the refund line. The "operating mode… phase diagnosis" description is the TEST product only; the live $9 product reads plainly. Still James's, in the Stripe dashboard: whether Klarna, Cash App and Amazon Pay belong on a $9 checkout.
- **Result page has two $9 buttons** now: a compact one under the salary/timeline line (`placement: "result_top"` on its `begin_checkout`) and the original full pitch below. Compare them in GA by that param. `begin_checkout` is marked as a **Key Event** in GA (2026-09-03); the $24 flow now fires it too.
- Four Gmail drafts to the stalled Aug 19/20/23/25 buyers were created 2026-09-03 with their recover links — James sends.
- **Quiz follow-up automation (2026-09-03)** runs from the app, not MailerLite (its API cannot create automations). `/api/quiz-result` records each completion in Redis (`lib/quiz-log.ts`); `/api/cron/quiz-followups` (daily 15:30 UTC) sends a day-2 note (their own first move, range, caveat + the $9 link) and a day-6 note (one question, no pitch), plain and reply-to james@. Once per address via `claimOnce`, skips buyers (`customer:` flag set by both finalize routes) and opt-outs (`/api/unsubscribe`, HMAC-signed with `CRON_SECRET`, also flips MailerLite). Only completions from deploy day onward get follow-ups; earlier takers were covered by the 2026-09 quiz-takers campaign.
- **Result page collects the résumé before payment on desktop** (pointer:fine + ≥768px) so the report builds the moment payment clears; phones keep pay-first.
- **MailerLite merge-tag rule: defaults must be a single word.** `{$name|default(there)}` works; `{$quiz_result|default(one of the nine paths)}` broke the parser — the tag printed literally AND the greeting rendered empty. All 246 quiz takers have `quiz_result` set, so no fallback is needed there.
- **Campaign ids (2026-09-03):** quiz takers A `197639526096569919` → group `197639245931742469` (246, buyers excluded); reactivation batch 2 `197639523119662950` → group `197638948099458342` (500 newest not in batch 1 / quiz / customers) — **hold until batch 1's 48h checkpoint**. 
- **Aug 14 traffic wave = Organic Social:** 174 of 287 sessions on Aug 14–15, ~7s engagement — a share/post, the source of most of August's sales. Repeat it monthly.
- **Stripe is the only source of truth for sales.** MailerLite customer groups only record buyers whose finalize completed, so they undercount whenever fulfilment fails. Stripe showed 9 real sales Aug 9–25 when MailerLite showed 5.
- **Never run a live-mode E2E with a real card.** Both checkout routes set `allow_promotion_codes: true`: create a 100%-off live coupon, run the flow at $0.00, then deactivate it.
  - Anything touching a payment path is now a change to a money-handling system. Validate before it can reach a buyer, not after.
- Upstash Redis via `lib/stash.ts` (inputs + results, 7-day TTL). Not configured locally — payloads >450 chars fail checkout in dev; short test payloads fit Stripe metadata and work.
- Resend (`send.slptransitions.com`, verified) for transactional; MailerLite for marketing list (~2,500 subs, cold — see content/list-reactivation.md before ANY blast).

## Funnel reverted toward what sells (2026-09-22)
- **Zero sales Sep 17–22 after 8 in Sep 7–16** (6 × $9 report, 2 × $24 Suite; the Getting Started kit / Module 1 has never sold to a stranger). The Sep 15–18 changes had put the free course and the kit in front of every door. Reverted: the quiz result leads panic with the **$9 report on every device** again (the phone-to-course override in `CareerQuiz.tsx` is gone; `offerForStage` is the only rule), the product menus under the report and Suite offers list the paid alternative before the kit (`["suite","ground"]`, `["report","ground"]`), `/products` orders report, Suite, kit, and the app nav puts Career Quiz before Course. The course and kit stay the lead only for stages 1–3. **The WP header button is still "Course"; James is pointing it at the quiz in the Customizer.** Also note the Suite checkout refused every buyer from the Sep 21 sale deploy until the `STRIPE_PRICE_ID` fix on Sep 22. Judge the $9 prices a week from Sep 22, not before.

## Sale pricing (2026-09-21, James: "all products $9 for a limited time")
- **`lib/pricing.ts` is the only price source.** `LIST` holds list prices (report 9, suite 24, ground 19); `SALE = { on, price, note }` flips every surface at once; `priceOf(k)`, `wasOf(k)` (struck price or null) and `wasNote(k)` (" for a limited time, was $24") feed the quiz result, the Suite wizard and paywall, both results-page cross-sells, `ProductMenu` (strike-through), `/start`, the Ground page (strike-through on the price card), `Locked`, `Dashboard`, lesson 0.1, the ground page metadata and every email template. `GROUND_PRICE` in `lib/course-tiers.ts` is now `priceOf("ground")`; `GROUND_LIST_PRICE` is the list price. **Never type a price into copy again**; grep for `$24`/`$19` should only hit comments, GA item names and `customer_product` labels.
- **The checkout guard.** `assertPriceAmount(stripe, priceId, dollars, label)` in `lib/stripe-guard.ts` retrieves the Stripe price once per process and refuses to create a session if `unit_amount` differs from `priceOf()`. All three checkout routes call it. The buyer sees "The … price is being updated. Try again in a few minutes, and nothing has been charged."; the log has the price id and both amounts. Proven locally 2026-09-21: the $24 and $19 test prices were refused, the $9 report passed. **This means the Suite and Module 1 buttons refuse until the env vars point at $9 live prices** (`STRIPE_PRICE_ID`, `STRIPE_GROUND_PRICE_ID`, then redeploy). Ending the sale is the reverse: `SALE.on = false`, env vars back to the list-price ids, redeploy.
- **/products** (`components/ProductsPage.tsx`, noindex) is where the sale banner lands: the three tools via `ProductMenu`, GA `placement: products_page`. **`components/SaleBanner.tsx`** renders in `PageShell` only (not `CourseShell`, so a Module 1 buyer is not shown a cheaper price inside the course). On slptransitions.com the strip is the `SALE_BANNER` tuple at the top of `scripts/build_home.py` (homepage only; set it to `None` and re-run `python3 scripts/build_home.py home`, then purge). **Kadence element 3656** ("Sale banner", `_kad_element_hook: kadence_before_header`, `_kad_element_all_show: true`) was created over REST as the site-wide version and saved cleanly but never rendered, on the homepage or an article, with either that hook or `wp_body_open`, after purges; it sits in **draft**. If James opens Appearance → Kadence → Elements and publishes it from the admin, delete the homepage strip so there is one banner. Ending the sale: `SALE.on = false`, `SALE_BANNER = None`, env vars back to the list-price ids, redeploy, rebuild the homepage. The MailerLite popup "pop up" (form `105339631824799453`, 319 signups from 15,240 opens) cannot be deactivated over the API (a PUT is accepted and ignored); James switches it off in MailerLite → Forms → Pop-ups.

## Support address (2026-09-14)
- **`lib/contact.ts` `SUPPORT_EMAIL` is the only address a buyer is ever told to write to**, and `REPLY_TO` in `lib/email.ts` is the same constant. Never type an address into copy. **`hello@slptransitions.com` does not exist** (SiteGround answers 550, "no mailbox by that name") and the app had sent buyers there in eleven error states, including "email hello@ with your receipt" after a failed payment. **`james@slptransitions.com` is accepted** by the MX (SiteGround's spam gateway, which accepts any recipient, so an SMTP probe cannot tell you whether a mailbox exists) but on 2026-09-14 was not forwarding to jamoberges@gmail.com: zero mail delivered to that address had reached Gmail in 90 days, including James's own test. Fix is in SiteGround Site Tools → Email → Forwarders, not in code. Gmail does have `james@` as a verified send-as alias, so outbound is fine. SPF still lists Namecheap Private Email hosts, a leftover from wherever the mailbox used to live.

## Key flows / files
- `components/SLPCareerSuite.tsx` — main wizard. URL params: `?continue=<stripe_session>` rehydrates paid inputs via `/api/session-inputs`; `?from=quiz&path=<ROLE_OPTION>` pre-selects target; `+&goal=report` routes resume → REPORT_INTAKE → $9 checkout.
- `lib/quiz.ts` + `components/CareerQuiz.tsx` — in-app quiz (8 sectioned questions; experience-weighted scoring; **11 paths** each with exact `roleOption` chip mapping + icon; Sales/BD and Rehab Management/Leadership added 2026-09-03, ceilings all 18–22). Adding a path touches three coupled places: `PATHS` here, a matching chip already present in `ROLE_OPTIONS` (lib/companies.ts), and a new `QUIZ_PATH_GROUPS` entry + real MailerLite group — a missing group key fails silently. Score ceilings should stay in a tight band; compute them from source rather than by hand (they drifted to 14–22 before the 2026-08 rebalance, so Content/Marketing could barely win). Clinical Research / Study Coordinator is deliberately scored **-4** for anyone who needs to match their SLP pay — it is the one path whose documented range sits below where most SLPs already are. `/quiz/embed` = chrome-free iframe version for the WordPress career-quiz page (CSP frame-ancestors in `next.config.mjs`; height postMessage).
- `app/api/finalize` ($24) & `app/api/report-finalize` ($9): verify paid session → retrieve stashed inputs (`stash_key` metadata, NOT session id) → generate → email → cache result by session id. **maxDuration 300**; full generation runs as two parallel prompts (buildFullPromptParts: materials + guidance, disjoint keys merged with a spread) — ~93s wall vs ~140s single-call. 120s maxDuration once caused killed functions (no render, no email).
- `app/api/refine` — buyers rewrite 6 sections, 10× cap. `app/api/parse-resume` — server-side PDF/DOCX (unpdf/mammoth). `app/api/fetch-job` — job-posting URL → text (SSRF-guarded; LinkedIn/Indeed block readers).
- `lib/email.ts` — all transactional templates. `lib/companies.ts` — 188-company DB + matching (quote `COMPANY_COUNT`, never a literal); ROLE_OPTIONS (functions) vs INDUSTRY_OPTIONS (separate axis).

## Workflows
- **Dev**: preview server via `.claude/launch.json` (`slp-career-suite`, port 3000). Shell wrapper exports empty `ANTHROPIC_API_KEY` — launch config handles it; if running manually, `unset ANTHROPIC_API_KEY` first. Secrets in `.env.local` (gitignored).
- **Typecheck**: `./node_modules/.bin/tsc --noEmit` before every commit.
- **Deploy**: push to `main` → Vercel auto-deploys (~90s). Plain `git push origin main`.
  - **Auth is SSH as of 2026-08-29.** Remote is `git@github.com:jamerges/slp-transitions-resume-app.git`, key `~/.ssh/id_ed25519` (ed25519, no passphrase), `~/.ssh/config` pins it with `IdentitiesOnly`/`UseKeychain`, and GitHub's host key is pinned in `known_hosts` after verifying it against their published fingerprint. **There is no token any more** — SSH keys don't expire, so ignore GitHub's PAT-expiry emails and don't generate replacements. Three fine-grained PATs were burned before this by being pasted where secret scanning could read them; that loop is now closed. Never paste a PAT into chat regardless.
  - History, in case it resurfaces: auth used to be `osxkeychain` over an HTTPS remote. `~/.gitconfig` also carried per-host `[credential "https://github.com"]` / `[credential "https://gist.github.com"]` sections containing an empty `helper =` (a chain **reset**) followed by `!/tmp/gh_2.86.0_macOS_arm64/bin/gh auth git-credential`, a binary that no longer exists. Every push printed two "No such file or directory" lines and still worked only because the generic `[credential] helper = osxkeychain` sat *later* in the file and got re-added after the reset. Both sections were removed 2026-08-15 (backup: `~/.gitconfig.bak-20260815-130235`). If those noisy lines reappear, something re-ran `gh auth setup-git` from a temp install.
  - Verify prod by curling the API routes, not by hammering pages (tripped Vercel bot protection once).
- **Test generation quality** dev-only: create a temp `app/api/test-*/route.ts` gated on NODE_ENV=development, curl it, **delete before committing**. Quality checks: banned words, burnout leakage into documents, fabricated numbers, stage grounded in `transitionStage`.
- **Test purchases**: LIVE mode — a real card is a real charge. Use a 100%-off live promo code (`allow_promotion_codes` is on for both prices), then deactivate it. Card 4242… only works if you point env at test keys.

## State & other memory
- **Notion HQ** (boulders Now/Next/Later + to-dos kanban): https://app.notion.com/p/3ae8bea627688165859cd63024729aa4
- `content/` = all strategy docs, 12 ready blog posts, email sequences (13), distribution playbook, list-reactivation plan, WordPress page copy + quiz embed snippet, research-facts.md (**every content claim must trace to this file**), style-guide.md (James's voice).
- Auto-memory has the focus/ladder decision. Deploy emails to James come from `results@send.slptransitions.com`.

## Open items (also in Notion HQ)
- **Validate the pay-first $9 flow against live Stripe with a 100%-off promo code** — deployed 2026-08-01, never run end-to-end (Upstash isn't configured locally, so the post-payment resume step can only be exercised in prod). If `report-intake` fails, buyers pay and get nothing.
- James: swap Typeform embed on slptransitions.com/career-quiz/ for `content/wordpress-quiz-embed.html`; publish blog posts; MailerLite sequences; add buyers to a "Customers" group in finalize (code TODO)
- 30-day refund promised on paywalls — honor it

## WordPress publishing (added 2026-08-01)
- REST API works; auth = `WP_APP_USER` + `WP_APP_PASSWORD` (Application Password, in `.env.local`, **unspaced** — WP ignores the spaces and unspaced avoids shell-quoting issues). Note `.env.local` has a pre-existing multi-line value that breaks `source`; parse the WP vars directly instead.
- `scripts/wp_publish.py` = helpers (markdown→Gutenberg blocks, FAQ + FAQPage JSON-LD, quiz CTA group, media upload). `scripts/publish-day1.py` = the per-post config (category, CTA line, authored FAQs, internal links) + runner. Pass a slug as argv[1] to publish just one.
- Live cluster (2026-08-01): slp-transferable-skills 3389, slp-resume-non-clinical 3391, slp-cover-letter-non-clinical 3393, slp-linkedin-career-change 3395, should-you-quit-slp 3397. Remaining 7 posts in `content/blog/` are unpublished.
- Verify after publishing by curling the live URL: check FAQPage JSON-LD parses, featured image, quiz CTA, internal links, and that no raw markdown (`**`) leaked.

## MailerLite (fixed 2026-08-01)
- `lib/mailerlite.ts` is the single source of group ids + field names. **MailerLite silently ignores unknown `fields` keys** — that's how `quiz_result` was dropped for every quiz taker since launch (the field didn't exist on the account). Adding a new field here means creating it in MailerLite first.
- `QUIZ_PATH_GROUPS` is keyed by **`roleOption`** (from lib/quiz.ts), NOT the display label — they differ for Data Analysis, Instructional Design, and Content Strategy / Marketing. A key miss fails silently, adding no group.
- Root cause of the outage: Vercel's `MAILERLITE_API_KEY` (added Apr 29) predated the working token (created Apr 30), so production 401'd and skipped. A stray `Mailerlite_API_Key_quiz` var held a Stripe `sk_live_` key and was deleted.
- List shape: ~2,481 active, but only ~851 in "Ed and Health Tech List" and **~1,628 in no group at all**. Reactivation must be two-track — see content/list-reactivation.md.
- Buyers auto-join Customers groups from both finalize routes with `customer_product` set.

## Aug 2026 sweep — findings (Notion: "Action Plans — Aug 2026")
- **Secrets leaked in Notion.** The "Resume reviewer web app" sub-page holds plaintext Anthropic, MailerLite, Resend + Stripe test keys. Rotate before scrubbing; scrubbing alone doesn't un-leak.
- **Five finished interviews sit unpublished in Drive** (Katie Seaver ~90%, Jeannie Baranowski, Lucinda Bowman, Porcupine Coffee, Jon). Katie Seaver is also Rachel Levy's Babel Group co-founder and emailed Aug 2025 — still unread — offering a cross-post on their blog.
- **Biggest unwritten content gap is PSLF / student loans** — readers name it as the specific blocker, and there's zero coverage. Then part-time/fractional roles, remote-for-health, and "should I keep paying for ASHA."
- **Reader mail dries up after 2024**, matching the MailerLite outage. The list isn't just distribution — it's the voice-of-customer input.
- Dead links in old plans: rehabrebels.org now redirects elsewhere; slpburnoutcoach.com has an expired cert (both are named affiliate/pitch targets).
- The Notion to-do that said "real card, real $24, then refund yourself" has been corrected — it contradicted the live-mode rule above.

## Homepage (rebuilt 2026-08-29 — replaces the 2026-08-02 six-card design)
- **Generated by `scripts/build_home.py`** — edit that script and re-run it: `python3 scripts/build_home.py home`. The default build reproduces the live page byte for byte; keep it that way, or a regen silently reverts the homepage. Do NOT hand-edit in the block editor.
- Structure: hero (headline + quiz/resume CTAs + trust line + **three proof callouts**: 188 companies / 20 paths / $154k+) → **one pillar box** linking to the 20-paths article → dark story band (3 real headshots) → guides → single-CTA closer, then the MailerLite Universal block. `carry_over_mailerlite()` now preserves that block automatically; it used to be a manual re-paste.
- **The six career-path cards are gone** (James, 2026-08-29): the pillar article is already the comparison table. ⚠️ **This removed the amber Epic-sponsorship and UXR-oversaturation caveat boxes from the marketing site.** They still live on app.slptransitions.com/jobs/<path> and in quiz results, but NOT on the pillar article — if those guardrails are wanted on slptransitions.com, they need adding to `/alternative-careers-speech-pathologists-slps/`.
- Copy is ~396 words, down from 593. Subhead validates before selling ("You're allowed to want out" — the reader's own phrase from voice-of-customer.md) and names no reason for leaving, so readers whose reason is pay or flexibility aren't excluded.
- **No em-dashes.** build() rewrites them to `&#45;` entities on the way out, because wptexturize turns a spaced hyphen into an en dash. Number ranges keep their en dashes.
- **Every featured story needs a real headshot** — build() asserts it. Avatars are cropped from each post's `-hdr-v2` graphic (those are text cards containing a real circular photo; don't reuse the whole graphic as an image, it duplicates the copy). Never an AI-generated face.
- Motion: staggered fade-up reveals only, via a scroll-loop sweep (not IntersectionObserver — anchor jumps strand elements hidden). Gated on `.js-anim` so no-JS and reduced-motion get the static page. Do not reintroduce edge furniture or drawn lines.
- Palette anchored to the logo's emerald (#00A080 / #0B6B54 buttons / #0A3D31 dark band). Amber = warnings only. Kadence's `.entry-content` link colour fights `.slp-btn`; the CSS uses scoped `!important`.
- **⚠️ WordPress auto-adds any new page to menu 29.** Creating a preview page puts it in the live nav — delete the menu item after, or turn the setting off.
- **⚠️ SiteGround Speed Optimizer caches the whole site.** After any WP change, purge: `PUT /wp-json/siteground-optimizer/v1/purge-cache` (PUT only; GET/POST 404). Otherwise you and the client see stale pages and disagree about what shipped.
- Backups: `home_backup_pre_tight_20260829.txt` (pre-rebuild) and `home_backup_pre_redesign.txt` (the 2026-08-02 grid), plus WP revisions.

## Blog index (/blog) — generated page (2026-08-02)
- **`scripts/build_blog.py` renders /blog** (page 3459): featured latest post + "New" list of 5 + full-image grid, Osmind-inspired. The posts-page designation was removed (`page_for_posts=0`) because WP ignores content on a posts page. Kadence title band hidden via `_kad_post_title: hide` meta (REST-writable on pages).
- **⚠️ /blog does NOT auto-update. Re-run `python3 scripts/build_blog.py` after publishing any post.** It warns about posts missing featured images.
- Category chips link to /category/{guides,real-transitions,entrepreneurs,mindset}/ — those archives still use the stock Kadence template.

## Companies list — ONE source of truth (2026-08-02)
- **`lib/companies.ts` is canonical.** 260 companies as of 2026-09-22 (188 until then; 72 added from TIME/Statista's World's Top HealthTech Companies 2026, US and clinician-adjacent only, each carrying `source: "time-2026"`, as do the 33 existing entries that appear on that list; notes are the companies' own site descriptions, seven written by hand where the site blocked the fetch; `/companies` states how many appear on the TIME list with a link, a checkable claim, and the list is still "companies that value clinical skills", never "has hired"). **Every surface quotes `COMPANY_COUNT`** (exported beside the DB) rather than a literal: the count drifted to 120, 123 and 126 across the app, the emails and the WordPress homepage band before James settled it at the DB's real length. `scripts/build_home.py` derives the same number by counting the DB file. (History: 126 → 120 on 2026-08-16 after a link audit removed 6 unreachable entries and corrected Expressable/Athelas; then the list grew.) Re-audit with `scripts/probe_job_feeds.py`, which reports unreachable entries as a side effect. The old Airtable share link held the same rows in the same order and has been retired — do not re-introduce it. Archive it in Airtable so nobody edits a dead copy.
- **What the list is, in James's words (2026-09-12): "health and ed-tech companies that value clinical skills."** Not companies that have each hired an SLP. "Companies that hire former SLPs" stays as the lead magnet's established name, but never sharpen it into "has hired", "actually hired", "track record of hiring" or "documented history of hiring": all four shipped in the app and a video and were cut the same day. `roles` on a company is observed-at-some-point, so a per-path list is "companies that have listed roles like this".
- Rendered at **app.slptransitions.com/companies** (`app/companies/page.tsx` + `components/CompaniesDirectory.tsx`). Search + 5 category facets. `noindex` with canonical to the WP lead-magnet page so the two don't compete.
- **No role filter, deliberately.** The `roles` field is observed-at-some-point, not live openings. Filtering or badging on it implied a job board we don't run. If live postings ever get ingested (see the weekly-digest idea in the Notion action plan), it can come back.
- Delivery chain: WP `/ed-health-tech-jobs/` → MailerLite form `105146994009311053` (groot 9454261) → group **Ed and Health Tech List** `105147013209786016` → automation `105149559028582086` → email. The form's success block now links straight to /companies.
- **⚠️ The MailerLite welcome email still links to the retired Airtable URL** — it must be edited by hand in MailerLite (automation email content isn't writable via their API).
- WP gotcha: the success-block button is a hand edit inside the pasted MailerLite embed. Re-pasting the form code from MailerLite wipes it; there's an HTML comment in the page saying so.
- Dead domain found 2026-08-02: **freshslp.com now redirects to a spam site** and was linked from the Mattie Murrey-Tegels post. Link removed, correction note added. Re-check other outbound links periodically.- **Section order (settled 2026-08-03): hero → career paths → real transitions → guides → companies band → final CTA.** Career paths sits at #2 deliberately: it is the only mid-page section that does NOT link off the homepage, and it answers the question readers actually arrive with. Stories and guides are both exit links, so they come after. A stories-first variant was built, measured and rejected — it put the first off-site link 1,100px higher up the page.
- The translation strip (clinical phrase → business phrase) was removed from the homepage: it explained a mechanism before the reader had reason to care. Pairs still live in content/blog/03-slp-transferable-skills.md and in TRANSLATIONS in build_home.py.

## Email marketing state (2026-08-06)
- **First-ever sends prepared.** ⚠️ Corrected 2026-09-03: the MailerLite API **does** accept HTML `content` on `POST /campaigns` (create, `type: "regular"` required) — it silently ignores `content` on `PUT` (update). So build a campaign by creating it fresh, never by updating a shell. Audience comes back as `filter: [[{in_any, [groups, [id]]}]]`, not `groups`. Send with `POST /campaigns/{id}/schedule {"delivery":"instant"}`. Name fallback tag is `{$name|default(there)}`. The API rejects any `plain_text` key (create AND update) with a misleading "emails.0 must be an array" error — MailerLite generates the text part itself, and it reads "a newsletter from **jberges**" because that is the account name. Fix the name under MailerLite → Account settings, not in code. Sends are `POST /campaigns/{id}/schedule {"delivery":"instant"}`; status goes ready → sent in ~1–2 min. Kit: `content/email-campaigns/2026-08/` (README has the 15-min step list + 48h checkpoint thresholds).
- **Friday compass send (2026-09-18):** campaign `198988065869399750` ("A compass, not a clock", free Module 0 + $19 Module 1, reply for feedback) went to quiz takers A + the 11 quiz path groups + the three customer groups in one deduplicated send (MailerLite sends once per subscriber across groups). Script `scripts/campaign_friday_compass.py <group_ids,comma,separated> <name>`; copy in `content/email-campaigns/2026-09/friday-compass.md`; three test sends to the James-only group first. Batch 1 cleared its checkpoint (78% opens, 13% clicks, 0 spam, 4 unsubscribes, 1 hard bounce of 200) and got the compass email the same evening. **Batch 2 (group `197638948099458342`, 500 newest) has NOT had it**: send it a day later with `python3 scripts/campaign_friday_compass.py 197638948099458342 "2026-09-19 Friday compass — reactivation batch 2"` after checking batch 1's compass numbers.
- **FIRST CAMPAIGN EVER SENT 2026-09-03:** reactivation batch 1 (campaign `197631601187751061`, subject "What's new at SLP Transitions") to group `195115205810718060` (200 newest active non-quiz subscribers), via the API. **Do not send batch 2 until the 48h checkpoint passes: >25% opens and <0.3% spam complaints** (content/reengagement-sequence.md). Under ~15% opens → stop and rethink. Render-test recipe: group `197631028917961980` ("zz test — James only") holds only jamoberges@gmail.com; create a campaign against it via POST and schedule instant. Final approved copy: content/email-campaigns/2026-08/reactivation-batch1-FINAL.{txt,html}. The quiz-takers campaign is still unsent.
- Draft shells live in ML: quiz-takers (→ 8 quiz-path groups, ~19 ppl) and reactivation batch 1 (→ group "Reactivation 2026-08 — batch 1", exactly 200 newest active non-quiz subscribers, membership verified by listing; `active_count` lags — trust the listing).
- Deep link contract (verified in code + live): `app.slptransitions.com/?from=quiz&goal=report&path=<roleOption>` → enters $9 report flow with target preselected; unmatched/dummy `path` still enters the flow, just without preselect. `{$quiz_result}` merge field holds the roleOption.
- Group-assign API calls fail silently under rate pressure — assign, then LIST the group and retry the diff (103/200 landed on the first pass).
- Welcome automation stats at baseline (before email 3): E1 851 sent / 91% open / 71% click; E2 72% open. Zero campaigns had ever been sent on the account before this.
- James's sender identity for marketing: "James from SLP Transitions" <james@slptransitions.com>; intro line = former SLP → copywriter & content strategist at a mental-health-tech company.

## Pillar article = 20 paths (2026-09-03)
- Post 3358 was expanded from 13 to 20 paths by `scripts/update_pillar_paths.py` (idempotent; backup `content/flagship-post-backup-2026-09-03.html`). It now covers every path on thenonclinicalpt.com's 14-path list plus six they don't have (SpEd coordinator, UX, conversation design, software engineering, forensic SLP, instructional design). Telehealth is deliberately NOT counted: it is clinical work and appears in the part-time section as a bridge.
- The salary table was rewritten to research-facts.md figures. The old table undersold every path (liaison $70–95k vs the sourced $84–135k; informatics $71–97k "higher with Epic certification", which contradicted the sponsorship guardrail lower on the same page). New figures + sources are in research-facts.md under "Additional path figures (2026-09-03)". Keep that file and the table in sync.
- Counts live in three places: the article title, `scripts/build_home.py` (proof card + pillar box), and `scripts/make_guide_header.py` (header graphic text). All say 20 now. /jobs copy derives its count from `PATH_LIST.length`.
- Competitor content map (Rinki Varindani Desai: NCPT 14 careers, 9 archetypes, ASHA 7R) and the stage-validation plan that answers it: `content/mindset-stage-plan.md`. Her frameworks validate and carry no data; ours has data and thin validation. Close the gap by adding validation in front of the data, not by dropping the data.

## Quiz stage question, path cards, five-stages article (2026-09-03)
- `lib/quiz.ts` `STAGES`: five non-scoring "which sounds most like right now" options (private / guilt / permission / panic / action, from voice-of-customer.md). Question id `stage`, flagged `stage: true`. Stored in Redis completions (`st`) and MailerLite field `stage` (created 2026-09-03; unknown fields are dropped silently, so never rename it without creating the new one first).
- Result page: opener line by stage + a typographic card per path from `public/quiz/<slug>.png` (`scripts/make_quiz_path_images.py`, reads PATHS; re-run after adding a path or changing a range/timeline). The h1 is screen-reader only because the card carries label/range/timeline. Quiz is now 9 questions; the WP quiz CTA block text says 9.
- Day-2 follow-up (`sendQuizFollowupDay2`) branches on stage: private → five-stages article; guilt → five fears; permission → three stories; action → resume post + suite; panic/unknown → first move + $9 report. Stages 1–3 get no pitch on purpose.
- Timelines in PATHS must read after the word "typically" (lowercase start, no leading "Fast —").
- **Five-stages article** live: https://slptransitions.com/youre-allowed-to-want-out/ (post 3600, Mindset cat 25, header media 3599) via `scripts/publish_stages.py` (imports publish-day1's `publish()` and adds a POSTS entry). Category ids: mindset 25, guides 99, real-transitions 100, entrepreneurs 98.
- Refreshed welcome automation copy (7 emails, mindset-first) is in `content/email-campaigns/welcome-sequence-2026-09.md`; James pastes into MailerLite (automation content is not API-writable). The live automation still links the retired Airtable list.
- Buyer feedback emails: sent 2026-09-03 in James's voice to April (reply: free 30-min call, no coaching offer), bhakti360, Kristine, sjensen, Jessica.

## Practitioner Pivot bundle read (2026-09-21)
- `content/competitor-practitioner-pivot-bundle.md` is the read of Hannah Pugh's "Getting Started Bundle" (51-min webinar transcribed, 16-slide deck, 39-page networking guide, 3-page résumé checklist; files in `~/Downloads/Non-Clinical Getting Started Bundle! /`, personal-use licence, nothing reused verbatim). What shipped into the course the same day: 3.1 (the "what do you do for work" follow-ups, products-on-your-cart list, industry→company→person→posting search order), 3.2 (cheap-yes first message with two windows and a one-question fallback, `message-check` tool, the offer-back in the thank-you), 4.2 (swap-versus-translation example, the colleague test), 4.7 (`resume-checklist` tool, eight items), LinkedIn checklist (connections visibility, twelve items). **Not adopted:** her translations (they are the 4.4 slop), her stats (500+ connections, "LinkedIn is 100% required"), stock and generated faces, the dodge on salary.
- **Module 1 has zero sales at $19 and at $9.** The doc's diagnosis: it sells reflection where the market buys orientation (every competitor's rung is called Getting Started), the concrete outputs are free, the name says "not yet", there is no face or proof. **Proposed repackaging, James's call:** rename for the stage, make the paid tier Module 1 + 3.1–3.3 + 4.2/4.7 + the stage map (a per-lesson `canOpen`), hand over two more PDFs (translation pass, who-to-talk-to-first), rewrite the sales page around "most SLPs start at the résumé" with the eight stops as one graphic and the four FAQ questions answered with our numbers, and **record a 25-minute webinar**: script at `content/course/getting-started-webinar-script.md`, built from research-facts and James's real biography only.

## Writer kit (James, 2026-09-04)
- `content/writer-kit/` holds James's own voice, style, audience, examples and anti-slop references (from his compound-writing workspace). **Every headline, lesson title, email subject and article in his voice follows it**, on top of style-guide.md 8a/8b. Short version: curious not authoritative ("I used to think…", "I noticed…"), self-implicating, SLP-native detail (caseloads, IEPs, productivity, CF year, grad debt), specifics over polish, humour by dry specificity, no coach-from-the-mountaintop, no "it's not X it's Y", and the portability test: if a teacher site could publish the sentence unchanged, make it more specific or cut it.

## Transition OS review notes (2026-09-04)
- The "hours outside work" dial/question was removed (it read as two questions: job-search time now vs. hours in the new job). Dials are pay floor, distance from clinical, people-time, tools. `DIAL_PROFILES` has no `hours` key.
- Buttons keep one label forever; completion shows as a checkmark line. Every lesson ends with "← Quest log" and "Next lesson →".
- Path is optional and only personalises; guardrails (Epic/MSL/UXR) live in Module 2, lesson 5, not in Module 0.
- Copy rule for the course: lead with the point in a warm full sentence; no clipped contrast openers. Workbook: `scripts/make_course_workbook.mjs` → `public/course/module-1-workbook.docx`. No LibreOffice on this Mac and Pages refuses scripted export, so verify docx layout by opening it, not by rendering.

## Stage-gated quiz result + the map (2026-09-11)
- **Spike context:** 135 quiz signups on 2026-09-11 (vs 9–18/day), all via the quiz, flat across 22 hours (algorithmic share, not a blast; no campaign was sent). Stage split: panic 45, permission 30, guilt 27, private 20, action 7. Only ~5% action-stage, and both paid products need a résumé in hand.
- `lib/stage-map.ts` = one source of truth for each stage: name, "here", belief, truth, the one move (+ link), next stage. Copy echoes the five-stages article; **do not invent new moves, edit the article and mirror it.** `offerForStage()` decides what the result page and emails lead with: private/guilt/permission → map + no pitch (quiet text links only); panic → $9 report **on desktop only: on a phone the result page overrides panic to `map` (the $19) since 2026-09-15**, because the résumé-first report is where phone buyers stall (the email keeps `report`, device unknown server-side; compare `stage=panic` by `offer` and device in GA); action → $24 Suite ("includes the $9 report") with a just-the-report link; no stage (preset `?path=`) → report, the historical default.
- `components/StageMap.tsx` renders the five stops + you-are-here + one move + next. `app/quiz/map` is the printable sheet (stage + path slug in the URL only, never a name/email; `noindex`). Linked from the result page ("Print or save your map"), the result email, and every day-2 branch.
- GA: `begin_checkout` / `select_item` / `view_item` on the result now carry `stage` and `offer`; `placement` values: `result_top`, `result_offer`, `result_offer_secondary`, `result_quiet`. Compare conversion by stage before changing copy again.
- `lib/email.ts`: `renderQuizResultEmail()` and `renderQuizFollowupDay2()` return HTML without sending. To preview in dev, add a temporary `app/api/test-*/route.ts` gated on NODE_ENV=development that calls them, screenshot, delete before committing (done that way 2026-09-11).
- Day-2 follow-ups for the 2026-09-11 cohort fire 2026-09-13 15:30 UTC from `/api/cron/quiz-followups`; day-6 on the 17th. Check the ops alert that morning for the send count.
- Where to find the referrer for a spike: GA4 → Realtime → "Users by first user source" + "Views by page" (tells you WP `/career-quiz/` embed vs app `/quiz`). Facebook/Instagram usually show as direct. The local `.env.local` Stripe key is **test**; live sales are only visible in the Stripe dashboard or Vercel env.

## "Getting Started for SLPs" — the first-month kit (renamed and widened 2026-09-21; was "Before You Start Looking", Module 1 alone, which sold nothing)
- **What the `ground` product opens now:** Module 1 (seven lessons) **plus** `KIT_LESSONS` in `lib/course-tiers.ts` = 3.1, 3.2, 3.3, 4.2, 4.7, plus `/course/tracker` and the workbook route. `canOpen()` is still module-level (the full program); **`canOpenLesson(id, moduleN, access)`** adds the per-lesson allowance and is what the lesson page, the Dashboard (`next`, chips, "3 of 8 lessons are in your kit") and `LockedLesson` (`lessonId` prop) use. Adding a lesson to the kit is one array entry, then the `KIT.parts` copy on the same file, which feeds the sales page.
- **Sales page (`GroundBuy.tsx`) is built on the diagnosis**, "Most SLPs start at the résumé. That is why fifty applications turn into two screens", with an eight-stop `KitMap` (free / kit / part / later), then **the four FAQ questions answered with research-facts numbers** (liaison $84k–$135k, 6–15 months, 113/7/1, referrals and the one-in-four-to-one-in-five no-reply), then Module 0 as proof, then "What $9 buys" in three parts from `KIT.parts`, then the price card. Button text is "Get the kit". No testimonials still; proof rule unchanged.
- **Two kit sheets** ship beside the workbook: `content/course/workbook/kit-translation-pass.pdf` and `kit-who-first.pdf`, printed from static HTML in `scripts/kit-sheets/` by `scripts/workbook-render/render.sh` (one page each; check the page count after any edit). `/api/course/workbook?f=translation|people` serves them to ground and os; resources `R.translationSheet` (4.2, 4.7) and `R.peopleSheet` (3.1).
- Every surface that said "Module 1", "seven lessons" or the old name was rewritten in the same commit: ProductMenu, CareerQuiz result card, Dashboard (visitor panel, NOTE strings, coming-soon card), Locked, LessonPage note, lesson 0.1, the ground page metadata, the three email templates, the finalize and webhook labels, and the Module 0 CTA block on WP posts 3358/3251/3600 ("The Getting Started kit is $9 if you want to keep going"). Route, env var, Stripe metadata and MailerLite group still say `ground`; do not rename those.
- **The original notes below describe the product before the widening** and remain accurate for access mechanics, money path, waitlist mode, owner tooling, workbook editions and the free tier.

## "Before You Start Looking" — Module 1 + workbook for $19 (deployed 2026-09-12, WAITLIST MODE until the live price exists)
- **Name:** buyer-facing name is **Before You Start Looking** (`GROUND_NAME`/`GROUND_SUB` in `lib/course-tiers.ts`); "Ground" and "Explore" stay as module names inside the course. Route, env var and metadata still say `ground` — that is the internal id, do not rename it. Named for BOTH audiences: stages 1–2 aren't sure they want to leave, stage 3 is sure and doesn't know where to start. "Should You Stay?" was rejected because it mis-sells to stage 3 (30 of 135 on 2026-09-11).
- **Scope: Module 1 ONLY, plus the companion workbook.** Module 2 was in the bundle for a few hours and was pulled: it overlaps the $9 Pivot Report (both answer "which direction"), and lesson 2.6 inside the paid bundle literally sells the $9 report, so a buyer hit an upsell for something they'd just paid for. **Do not re-add Module 2 without solving that.** Mindset is the gap nothing paid covers, and it feeds the report rather than competing with it.
- **Price: $19** (`GROUND_PRICE` in `lib/course-tiers.ts` is the single source; test price `price_1UErFiKyPrmclvwmJlcQC7fE`). Chosen DOWN not up: it credits in full toward the $497, so its job is deposits, not margin, and stages 1-3 are the least committed buyers on the list. Also disambiguates from the $24 Suite. Ladder now reads free quiz / $19 decision / $9 paths-from-your-résumé / $24 application.
- **`PathMap` personalisation (2.1, now in the full program not the $19):** `PathMap` (2.1) reads Module 1 — `shared["1.5"].top` (dials), `shared["1.4"].energy` via `ENERGY_PATHS` in lib/course.ts, and `shared["0.2"].floor` — then marks paths "Fits you", says *3 of the tasks you marked as energising point here*, flags anything under the income floor, and offers "Show only these". **If you edit the energy audit or the dials, keep `ENERGY_PATHS` and `DIAL_PROFILES` in sync or the map quietly stops agreeing with Module 1.**
- **What it is:** Modules 0–2 of the course sold on their own for the stage 1–3 quiz takers (private/guilt/permission), who have no résumé in hand and whom neither existing product fits. Read-and-do, no video (the two video slots in those modules run as their scripts). Module 0 is free for everyone; the $19 opens Module 1; the full program opens 2–7 (`lib/course-tiers.ts` `canOpen`).
- **Access, no accounts:** `lib/course-access.ts`. A signed token (HMAC with `CRON_SECRET`, carries the Stripe session id, never an email) sets the `tos_access` cookie via `/course/unlock?k=…`. `getCourseAccess()` reads it server-side; every `/course` page is `force-dynamic`. Rotating `CRON_SECRET` invalidates all links/cookies; buyers recover at `/course/welcome?session_id=…`, which re-issues from Stripe. Refunds: `charge.refunded` in the webhook → `revoked:<sid>` in Redis + clears the OS credit. **Make sure the Stripe webhook endpoint is subscribed to `charge.refunded`** (it currently listens for checkout.session.completed + payment_intent.*).
- **Money path:** `POST /api/ground-checkout` (needs `STRIPE_GROUND_PRICE_ID`; refuses to run without it) → Stripe → `/course/welcome` → `POST /api/ground-finalize` (idempotent; sets the cookie on the response, emails the link once via `claimOnce`, `markCustomer`, `markGroundBuyer`, MailerLite group `CUSTOMER_GROUPS.ground` = `198364363480893430`). The webhook's `fulfil()` branches on `metadata.product === "ground"` and calls the same finalize, so browser and webhook paths agree.
- **OS credit:** `ground:<email>` in Redis holds `{cents, sid}` with no expiry (`groundCreditFor()`). The OS checkout, when built, reads it and attaches a coupon for that amount server-side. Fallback for a different email at OS checkout: a single-use code emailed to the Ground customer group.
- **Test mode:** product `prod_VF9eaz4vvqzhzF`, price `price_1UEfLQKyPrmclvwm04D8JjvY` (in `KNOWN_TEST_PRICES`). Local `.env.local` has `STRIPE_GROUND_PRICE_ID` (test) and a local `CRON_SECRET`. End-to-end passed 2026-09-11 with Playwright + card 4242 (`scratchpad/ground-e2e.mjs`): locked/free/unlocked states, welcome, idempotent finalize, fresh-browser link, tampered link rejected.
- **Owner tooling:** `GET /api/course/access-link?product=os` with `Authorization: Bearer <CRON_SECRET>` mints a full-program link (review prod without buying); `?product=ground&sid=cs_…` re-issues a buyer's link. Curl it from a terminal; never paste the secret in chat.
- **Funnel:** quiz result + result email for stages 1–3 lead with Ground (`placement: result_after_map`, item_id `ground`), the $9 report as the résumé-first alternative; day-2 branches for stages 1–3 carry one Ground line after the map.
- **`/course/ground` page anatomy (2026-09-12, built to `product-copy/playbook.md` §32):** hero = name + `GROUND_SUB` + one mechanism line (read-and-do, ~50 min, **no résumé needed**) + a value CTA anchored to `#buy`; then a stage card if `?stage=` is set (belief from `STAGE_MAP`, so the page continues the quiz result); the approved "This is the deciding part" paragraph; **Module 0 as the proof block** ("Try the free part first") with the one documented author line; the workbook card with `public/course/workbook-page.png`, a real render of the sunk-cost page (regenerate from the scratchpad `wbpage.html` + playwright + `sips -Z 900` after any workbook redesign); the eight lessons; "Ready to find clarity?"; the price card; the product menu. (A five-question FAQ was built and James cut it the same day.) **Proof rule: only what we have clearance for.** No testimonials exist for this product yet, so the proof is the free module, the real output and the author line. Do not add quotes, counts or logos. Note: the only model call in the course is the Module 5 mock-interview coach, so Module 1 has no AI in it.
- **Waitlist mode:** `/course/ground` checks `STRIPE_GROUND_PRICE_ID` at request time. Missing → an email capture into MailerLite group `WAITLIST_GROUPS.ground` = `198391691796809364` ("Waitlist · Ground (opening notice)") via `/api/ground-waitlist`. Present → the $24 button. Env changes need a redeploy (empty commit works). When it flips, send the waitlist group the link.
- **To go live (James):** (1) Stripe → Products → add "Ground: Week 1 of Transition OS", $24 one-time, copy the live price id; (2) Vercel → env → `STRIPE_GROUND_PRICE_ID` (exact name, Config, no trailing newline), redeploy; (3) push this branch of work; (4) validate with a 100%-off live coupon at $0.00, then deactivate it; (5) confirm `charge.refunded` is on the webhook endpoint. Until (2) exists, the buy button errors safely and nothing is charged, which is why the commit is held rather than pushed.

- **Gotcha:** the progress store key is `tos:progress:v1` (colons, not dots) — seeding it wrong in a test makes the course silently render as a brand-new visitor.

- **Product menu:** `components/ProductMenu.tsx` lists all three paid products with price and the question each answers, marking the one that matches the reader's stage. It sits under whichever offer the quiz result led with (all three branches) and on the Ground page, so the stage guess never hides an option. GA: `placement: result_menu` / `ground_menu`.
- **Workbook links follow the workbook's own pages.** It was rebuilt 2026-09-11 around three purposes, so it no longer has pages for the five stages, the decision tree, the energy audit, the dials or the twenty paths. `R.workbook1` (Word) and `R.workbookPdf` (PDF) are attached ONLY to 0.2, 1.3, 1.6, 1.7, 1.8 and 6.5, which are the lessons with a matching page. **If you add or remove a workbook page, update those resource lists or the course links people to a page that isn't there.**
- **Workbook is gated and comes in two editions.** Source `scripts/make_course_workbook.mjs [module1|full|both]`; output goes to `content/course/workbook/` (NOT `public/`, where it used to be downloadable by anyone with the URL). `/api/course/workbook?f=pdf|docx` reads the access cookie and serves the edition that matches: `ground` gets the 10-page Module 1 edition (cover, contents, the two 0.2 pages, weeks 1/6/12, the four Module 1 pages and Kept private), `os` gets the 16-page full one, which adds the twenty-minute conversation, interview prep, the offer and six months in (Modules 3, 6 and 7). No cookie gets a 403 pointing at /course/ground. `next.config.mjs` `outputFileTracingIncludes` ships the files into that function. **Regenerate BOTH editions and both PDFs after any workbook edit** with `sh scripts/workbook-render/render.sh` (no LibreOffice on this Mac; see the 2026-09-18 note below for the clipping caveat).
- **Workbook design system (2026-09-12, James picked it):** emerald field over a cream foot on the cover, Playfair Display headings falling back to Georgia (Word and the PDF renderer both get Georgia), DM Sans body, 16px/12pt reading-copy floor, the eight module colours only on the stops strip. Canvas: https://claude.ai/code/artifact/8864761b-b286-4e45-9c0a-85f777683220. The docx band sits inside the 1in margins rather than bleeding to the page edge, which is a Word limitation and also what a home printer can actually print.
- **Workbook page names say the outcome, not the mechanic.** "The twenty-minute conversation" became "Informational interview sheet", "The offer conversation" became "Negotiating the offer", "The sentence you tell yourself" became "Why leaving isn't a wasted degree". Titles live in `scripts/make_course_workbook.mjs` (blank) and `components/course/MyWorkbook.tsx` (filled) and must agree. Lesson 1.7 is still called "Tell one person" in `lib/course.ts`, which is deliberate: it is an action in the course and a page in the workbook.
- **Two workbooks, and they are different things.** `/course/workbook` (gated page, `components/course/MyWorkbook.tsx`) renders the buyer's OWN answers from the progress store into seven printable sections, with blank lines where no answer exists, and prints to PDF from the browser. That is the headline deliverable and what `R.workbook1` points at. `/api/course/workbook?f=pdf|docx` is the BLANK copy to write on by hand. Both are gated. If you add a Module 1 lesson that saves an answer, add it to MyWorkbook or it silently never appears.
- **No videos anywhere.** There is no footage, so `VideoSlot`, the "Recording slot: James" posters and the "Script" headings are gone, 19 lessons typed `video` were retyped, and `TYPE_LABEL` renders both video and explainer as "Read". Do not reintroduce a Video tag until a file actually plays.
- **Interactive lessons auto-save.** 0.2, 0.3, 1.4 and 1.5 save on change in a `useEffect` and call `finish()` themselves; there are no Save buttons. `TellOne` (1.7) and the checkpoint (1.8) keep a button because those are actions, not saves. Watch the XP-double-pay trap (CLAUDE.md, course-progress `useRef` mirror) when adding another.
- **Every door to the $19 leads with the free Module 0 first (2026-09-17).** `PRODUCTS.ground` in `ProductMenu.tsx` carries `button: "Start free"` (was "Find clarity, free" until 2026-09-21; no price sits beside a free door), which replaces the verb-plus-price button; the $19 is named in `detail`. `/start` and the result-page menu send the ground card to `/course` (free), while the result page's stage-aware lead card still goes to `/course/ground?stage=` because that page continues the quiz result. Three articles carry a Module 0 CTA block (marker `<!-- slp-module0-cta -->`, appended after the quiz CTA, not replacing it): the 20-paths pillar 3358, five hidden fears 3251 (which had no app link at all before), and the five-stages 3600. Copy rule from James for these: concrete outcomes ("know the number your next job has to clear, the date, which belief"), never "start with the free part".
- **What the Teacher Career Coach audit changed (2026-09-18; the audit is `content/competitor-teacher-career-coach-course.md`).** Lesson **0.2** is now the money page and saves `salary, hours, weeks, hourly, expenses, taxPct, floorDollars, trades` beside `floor`/`date`; `PathMap` flags any path whose band top sits under `floorDollars` as well as under the median when floor 0 is chosen. The 7.65% payroll-tax figure is the only statistic on that page; the tax slider is the reader's own assumption. **1.7** carries a six-item keep-it-private checklist saved as `answer.private[]` (LinkedIn's "Share profile updates with your network" and Open to Work "recruiters only" are real settings; don't rename them). **2.0** is a new lesson (`content/course/modules/module-2.json`, tool `time-budget`, saved as `shared.time`) on hours, the 60/30/10 split (a recommendation, not a statistic) and the applying window. **4.3**'s number-mining prompts are SLP-native now, and `numbersAsText()` in `components/course/tools.tsx` turns them into a block the Suite offers to append under the résumé when it finds `tos:progress:v1` in the same browser (`?from=course` or not). **3.1** opens with the warm ask, **3.4** carries the LinkedIn-hygiene callout, **6.6** has the two form questions (references, expected salary) and the follow-up rhythm, **6.8** has the answer-to-a-no script. **Two buyer-only report forms** (`questions-asked` in 6.4, `landed` in 7.1) POST to `/api/course/report`, which needs the access cookie and emails the ops inbox keyed by session id: that inbox is the interview-question bank and the alumni salary sheet until there are enough rows to automate. `MyWorkbook` shows the 0.2 numbers and the privacy count; the blank docx gained matching pages on 2026-09-18. Claims deliberately left out for want of a source: the "42% of postings close within two weeks" line that lives only in the pillar article, and any percentage about how many jobs come from networking.
- **Scripts by path and the second TCC pass (2026-09-18).** `content/course/scripts-by-path.md` holds twenty scripts adapted to the eleven paths; `content/course/course-improvements-2026-09.md` is the ranked list of what is still worth doing and records what shipped. New block type **`bypath`** (`{type, title?, note?, items: {slug: text}}`, keyed by `lib/quiz.ts` slugs) renders one copyable script per path with the reader's own path (0.2 or top dial) open first; used for the sixty-second pitches in 3.2 and the headlines in 3.4. Module 3 gained the who-first list, the conference section, the connection-request and alumni scripts, the broadcast-off warning before the headline rewrite, recruiter vetting and the check-for-an-in habit; 5.1 gained "ask ten people whether the certificate mattered"; 6.1/6.2/6.3/6.5 gained the interview scripts; the 6.8 offer checklist is **eight** items (vesting added) and the 3.4 LinkedIn checklist is **eleven**. **The workbook renderer now lives in the repo:** `sh scripts/workbook-render/render.sh` rebuilds both docx editions and prints both PDFs through the headless Chromium that Playwright left in `~/Library/Caches/ms-playwright` (no Playwright package is installed; `docx2html.py print` draws fixed Letter sheets with the header and footer, so **a page that overflows is clipped, not spilled**: check `.inner` scrollHeight against clientHeight in a browser after any page edit, as done 2026-09-18). Editions are now **10 pages (Module 1)** and **16 pages (full)**: 0.2 has "Your starting line" and "What you would trade", 1.7 has "Kept private". Workbook resource lists on 0.2 and 1.7 already pointed at the workbook, so nothing else moved.
- **Simplification pass (2026-09-18).** One tool per job, one place per number, one sentence per fact. **/course/tracker** (`components/course/Lists.tsx`, gated like Module 3) is the one home for the people list and the application tracker; lessons mount them only where the action happens (3.3, 3.6, 4.7) and link elsewhere through `R.lists`. Cut: lesson 2.7 and the `path-deep-dive` tool, the `screening-questions` tool (its six questions are a list in 6.3), the streak display (data still saved), the goal box in the people list (derived from 2.0's hours: `max(2, min(5, round(hours*0.3*2)))`, 3 if unset), two prompts from the kit. 5.3 merged into 5.2 ("Pick one thing and make it in a week", type action, badge `proof-artifact` now watches 5.2). `lib/course-progress.ts` `tidy()` renames a saved 5.3 to 5.2 and drops ids that no longer exist so the percentage stays honest. Lesson lengths after the pass: 300 to 1,400 reader-visible words; her lessons run 656 to 6,668 spoken words, so ours are in the ballpark and should not be padded or gutted. Do not mount a tool twice; link to /course/tracker instead.
- **Free-tier link (2026-09-18).** `CourseProduct` now includes **`free`**: a signed link minted by `POST /api/course/free-link` from the email typed into `components/course/SaveMyPlace.tsx` (end of lesson 0.4, and the visitor dashboard aside once a lesson is done). It opens nothing paid (`canOpen` unchanged) but carries a stable session id, `free_` + HMAC(email), so Module 0 progress syncs through `/api/course/progress` like a buyer's, and asking twice keeps one store. The route sets the cookie on the answering response and reloads the page so the browser copy is pushed up, emails `sendFreeLinkEmail` with the floor, date and verdict in it, and adds the address to MailerLite group `COURSE_GROUPS.free` = `198976109575931265`. A buyer submitting it keeps their paid cookie. Every gate that meant "paid" now checks for `free` too: the workbook route and the Module 1 summary route return 403 for it; `Dashboard` treats `free` as a visitor (`NOTE.free`), the lesson page and `LockedLesson` accept it as `owns`. GA: `generate_lead` with `placement: course_free_link`. The duplicate WP "Course" menu item was deleted the same day once the Kadence header button became "Course" (James did that in the Customizer); menu 29 is now Career Quiz · Jobs & companies · Blog · About · Contact plus the button.
- **The course is the front door (2026-09-18, James).** WP menu 29 is now **Course** (→ app /course) · Career Quiz · Jobs & companies · Blog · About · Contact; "Start here" (item 3641) was removed from the nav only. `/start` still exists for article and email links and is linked from the visitor course dashboard ("Pick your stage and see"), which also carries the product menu for the $9 report and $24 Suite (`placement: course_menu`), so neither product depends on the quiz result alone. The app's own nav in `components/ui.tsx` mirrors the menu. **Still James's, in the Customizer, not the API:** the Kadence header button ("Translate your résumé" → the $24 Suite) should become "Course" → https://app.slptransitions.com/course; `kadence_theme_config` is empty over REST so it cannot be set from here. The quiz result already recommends the product and the article by stage, so the quiz is the other door and nothing new was built for it. The purchase email subject is now "Your Transition OS login link" so buyers can search for it.
- **"Find your course link" and the WP Course nav item (2026-09-18, James lost his own way in from slptransitions.com).** There are no accounts: the signed link is the login, and `/course/find` (`components/course/FindCourse.tsx`) asks for the purchase email and POSTs `/api/course/find-link`, which lists paid Stripe Checkout Sessions by `customer_details[email]`, keeps `metadata.product` in {os, ground}, skips revoked ones, prefers os then newest, mints a fresh token and sends `sendCourseLinkEmail` to that address only. **It always answers the same sentence** whether or not a purchase exists (no enumeration), and is rate limited 3 per address and 10 per IP per hour via `rateLimit`. Linked from the visitor dashboard panel, the Module 1 locked page and the Ground buy card ("Already bought it? Get your link sent again"). The Ground access email now says progress is saved to the purchase, not the browser. **WordPress menu 29 gained "Course" → app.slptransitions.com/course** (item 3654, order 3; backup `content/wp-backups/menu-29-20260918.json`), so a buyer can get from the marketing site to the course and, if they have no cookie, to the resend page in two clicks. Sync check in prod passed the same day: James's Chrome showed "Progress is saved to your purchase."
- **Buyer progress syncs to Redis (2026-09-18).** `lib/course-progress.ts` still keeps localStorage as the working copy for everyone, and for anyone with the access cookie it also GETs `/api/course/progress` on mount, merges (newer document by `updatedAt` is the base, `completed`/`actions`/`badges` unioned, XP max, `answers` and `answers.__shared` merged per key so a deletion on the newer side wins), writes the union back, then PUTs 1.5s after every commit and beacons on tab hide. Store: `course:progress:<sid>` in Redis via `lib/quiz-log.ts`, no expiry, 400KB cap, keyed by Stripe session so it survives a cleared browser and a second device; free visitors get 403 and stay browser-only. The hook returns `synced` (null unknown, false browser-only, true stored) and the people list and the shell note say which. **Locally there is no Upstash, so `store` comes back false and the note reads "Saved in this browser"; the round trip was verified only by contract (403 without cookie, 200 with) and needs one check in prod: open the course in two browsers with the same access link.** Rotating `CRON_SECRET` does not lose progress (the key is the session id, not the token).
- **Networking CRM and the model lesson (2026-09-18, James: "her course was made before LLMs").** `contact-tracker` in `components/course/tools.tsx` is now a real people list shared across Module 3 (`shared.contacts`, legacy rows migrate on read via `stageOf`/`dueOf`): source, stage, **log a touch** (sets stage and the next-touch date: +8 days after a first message, +2 after a reply, +14 after a call, +30 after an update), a Due panel, per-person notes (what I learned, who they named, link), the 3.2/3.6 scripts copied with the person's name filled in, a weekly goal (`shared.contactGoal`, default 3) with a sent-this-week count from the log, and **Download as a sheet (CSV)** whose columns match `public/course/transition-os-tracker-people.csv`. `contact-tracker-warm` is the same tool with 3.6's finish (two rows at spoke/warm/referred). **Lesson 3.8** (`lib/course.ts` + `module-3.json`) is the ChatGPT/Claude lesson: hand over research, first drafts, rehearsal and tidying; keep the specifics, the send and the judgement; never paste client or employer material; it invents names and numbers. Its `prompt-kit` tool renders eight prompts prefilled with the path and `shared.numbers` from 4.3, each asking the model to interview the reader before drafting (4.4's rule). No model call is made from the app for this; the reader pastes into their own account. **3.6's volume position changed:** two warm conversations stay the floor, plus two new messages a week while building and five while applying.
- **The verdict lesson is free (2026-09-17, James's call).** "Bad workplace, bad fit, or bad season?" moved from 1.2 to **0.3**, and "Three things I believed" from 0.3 to **0.4**, so Module 0 is four lessons, about twenty minutes, and the diagnostic a Facebook visitor actually came for. Module 1 is now **seven lessons, about forty minutes**, and its promise is "your reasons in writing" (degree worth, energy, dials, identity, tell one, your why), not "whether you're leaving". Every count and promise was updated in the same commit: Dashboard, Welcome, ProductMenu, GroundBuy (hero, "What $19 buys", the lesson list), Locked, CareerQuiz, the three emails in `lib/email.ts`, the ground page metadata, and the Module 0 CTA block on the three articles. `lib/course-progress.ts` `migrate()` renames saved `1.2`/`0.3` keys on load (old three-lies is a completed 0.3 with no answer; the verdict always saves one), so buyers keep their verdict and badge. Anything reading the verdict uses `answers["0.3"]` now (Dashboard tag, MyWorkbook, Checkpoint1, the verdict badge).
- **A visitor's `/course` shows three things, not 48 lessons (2026-09-17, for the free-Module-0 Facebook push).** `Dashboard.tsx` branches on `visitor` (no access cookie): Module 0 marked Free, Module 1 with the $19 chip, and Modules 2–7 collapsed into one "Full program · Coming soon" card; badges, streak and the buyer resource rail are hidden and a single Module 1 panel takes their place. Buyers get the whole map. The full program is genuinely not purchasable yet (no OS checkout), so "coming soon" is accurate; retire the card when it is. Lesson 0.1 is written for the free module (three lessons, no card, no refund line: nothing was paid). **Free visitors never get a workbook**: both editions check the cookie, so 0.2's resource links say "opens with Module 1" and point at `/course/ground` when `access` is null instead of bouncing to a sales page or a 403. If you add a gated resource to a Module 0 lesson, keep that check.
- **Never sell a buyer what they already own.** `LockedLesson` and the Dashboard lock chips take the access product: a Ground owner hitting Modules 2–7 sees "the rest isn't open yet, what you paid comes off it" with links to the quest log and their workbook, NOT the $19 page, and the quest-log chips read "Opens after Module 1" with no href. Only a visitor gets the buy link. If you add a lock surface, pass `owns` through or it will sell Module 1 back to someone who finished it.
- **Finishing Module 1 emails the answers.** The checkpoint POSTs a summary to `/api/course/module1-complete`, which verifies the access cookie, reads the buyer's address **from the Stripe session** (never from the request body), and sends once per purchase via `claimOnce("m1summary:<sid>")`. The email carries every answer plus links to the filled workbook, the blank copy and the quest log, so the work survives the browser it was done in. `renderModule1SummaryEmail()` renders without sending, for dev preview.
- **`/start` is the no-quiz front door** (`components/StartHere.tsx`). Two of three paid products were reachable only from a quiz result, so anyone arriving from an article or email could not find them. Pick a stage, get the same recommendation the quiz result gives, plus the other two products and the free tools. Reuses `STAGE_MAP`, `StageMap` and `PRODUCTS`, so prices and positioning live in one place. `offerForStage` returns `"map"` for stages 1–3 meaning "don't pitch"; this page already leads with the map, so it surfaces Ground for those. `noindex, follow` so it doesn't compete with slptransitions.com.

## WordPress nav consolidated to point at the app (2026-09-12)
- Menu **29** (primary + footer) is now: **Start here** → `app.slptransitions.com/start` · **Career Quiz** → WP `/career-quiz/` · **Jobs & companies** → `app.slptransitions.com/jobs` · **Blog** · **About** · **Contact**. Backup of the previous six items: `content/wp-backups/menu-29-20260912.json` (restore by POSTing each `title`/`url`/`menu_order` back to `/wp-json/wp/v2/menu-items`).
- **Open Roles + Companies List merged into one item.** "Companies List" (item 3357, which pointed at the WP lead-magnet page `/ed-health-tech-jobs/`) was removed **from the nav only** — that page, its MailerLite form and its automation are untouched and still linked from articles and the homepage. Because the merged item points at `/jobs`, `app/jobs/page.tsx` now carries a link to `/companies`, or the list would be stranded.
- The menu REST API works with `WP_APP_USER`/`WP_APP_PASSWORD`: `GET|POST /wp-json/wp/v2/menu-items?menus=29`, `DELETE …/<id>?force=true`. **Purge SiteGround after any menu change** (`PUT /wp-json/siteground-optimizer/v1/purge-cache`) or the old nav serves from cache.
- ⚠️ **SiteGround bot protection blocks headless browsers on slptransitions.com** — a Playwright screenshot returns a padlock page. Verify WP changes by curling the HTML or the REST API, never by driving a browser.
- **The quiz page is WP page 3352 at `/career-quiz/`** (title "Find clarity", menu 29 item 3354, iframe of `app.slptransitions.com/quiz/embed`). On 2026-09-23 its slug had become `app-slptransitions-com-course-ground` (a URL pasted into the slug field), which 404'd the header button ("Find my career path" → `/career-quiz/.`), post 3600 and the ASHA post; the slug was set back. Pages don't get old-slug redirects, so never rename it. Full link audit that day: every external 403 (Medium, Forbes, Inc., UX Collective, NCVS) loads in a real browser, so re-check those by browser, not curl. Backup of what was changed: `content/wp-backups/broken-links-20260923.json`.
- Still pointing at the $24 Suite: the Kadence header button ("Translate your résumé"). It is configured in the theme, not in menu 29, so it was left alone.
- **Companies list: gated on the way in, ungated once inside.** Public/marketing links (`/start`'s free list, the `/jobs` cross-link) point at the WP lead magnet `https://slptransitions.com/ed-health-tech-jobs/` because that form is a proven email source. In-product links (`R.companies` on course lessons, the dashboard's "included with the program") point straight at `/companies` — a buyer must never hit an email wall for something listed as included. Keep that split if you add a new link.

## Skills (now tracked in git as of 2026-09-12)
- `.claude/skills/` is version-controlled (`.gitignore` allows it; only `settings.local.json` stays local). Three skills: **product-copy** (anything a buyer reads: sales pages, in-app UI, lessons, emails, buttons, nav — built from James's corrections, read it before writing product copy), **slp-transitions-writer** / `content/writer-kit/` (articles and essays), **voice-of-customer** (check a draft against how SLPs actually speak), **format-remix** (platform content from proven formats). **`content/writer-kit/mechanics.md` (2026-09-17) is James's sentence-level style guide and sits under all of them**: answer first, no shell-noun labels, no em dashes, no hedging or meta-discourse, actor-plus-verb over nominalizations, pronouns resolved, emphatic word last, edit before sending. Every skill points at it; read it before writing anything a person will read, including replies in chat.
- **Seven design lenses from Owl-Listener/designer-skills (MIT, commit 9a6930c, added 2026-09-24), picked from 111 for this funnel:** `behavioural-analytics` (read a GA/Stripe dip; rule out a deploy, a tracking change or a moved denominator before blaming the page), `onboarding-design` (the first session after purchase and the free Module 0), `form-design` (quiz, email captures, résumé upload, report intake), `hicks-law` (how many doors and offers one screen shows), and three screen critiques: `critique-affordance`, `critique-visual-hierarchy`, `critique-information-density`. Each folder carries its LICENSE and SOURCE.md. **They are generic lenses and lose every conflict:** the words stay under `product-copy` and `mechanics.md`; there are no accounts, so ignore SSO/sign-up advice; our traffic is too low for A/B significance (why `a-b-test-design` was not installed); accessibility, general critique and UX copy come from the `design:` plugin, so their duplicates were skipped.
- **`product-copy` is the one that stops repeat mistakes. Read it BEFORE writing buyer-facing words, not as a review pass** — on 2026-09-12 a workbook cover and contents list were written as "design" work, skipped the skill, and reproduced a failure already documented in it. Any words a buyer reads are product copy, including pages inside a PDF, a workbook, or a design mockup. `playbook.md` beside it is the general system (Harry Dry / Demand Curve / VeryGoodCopy synthesis) for page structure, proof, objections, CTAs and states; SKILL.md lists where James's rules override it (sentence rhythm, invented examples, CTA person, proof clearance). Its five failure modes, in the order they occur: the copy talks about itself, it names the mechanic instead of the outcome, it is vague where it could be concrete, it runs long and narrates, it isn't how a person talks. It also carries the never-invent-James rule and the pre-ship checklist (read the rendered page, grep for internal notes, re-check numbers after a restructure, test the visitor / buyer / finisher states).
